info:
  name: Delete Doctor
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8000/api/patients/YOUR_PATIENT_UUID/
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
